/**
 * Where customers can reach a human.
 *
 * SUPPORT_EMAIL is intentionally empty until a support@ address exists on the
 * domain. Every surface that shows it renders the row only when it is set, so
 * filling this in is a one-line change with no layout work — and until then the
 * site never advertises a contact route that goes nowhere.
 */
export const SUPPORT_EMAIL = '';

/** Discord is the primary route today: no account needed, replies from a person. */
export const SUPPORT_DISCORD_PATH = '/discord';
